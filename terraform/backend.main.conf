# Production backend configuration
bucket = "terraform-045107234435"
key    = "youtube_video_generator.tfstate"
region = "us-east-2"

# Optional hardening (enable when backend bootstrap resources exist):
# encrypt        = true
# dynamodb_table = "terraform-state-locks"