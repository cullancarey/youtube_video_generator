name: 'Build'

on:
  push:
    branches:
      - main

jobs:
  upload-artifacts:
    needs: [build-tweet-lambda-package, build-youtube-lambda-package]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install dependencies
      shell: bash
      run: |
      ./create_youtube_lambda_package.sh
      ./create_tweet_lambda_package.sh

    - uses: actions/upload-artifact@v3
    - name: Upload Build Packages
      with:
        name: lambda-builds
        path: |
         ../tweet_youtube_video.zip
         ../youtube_video_generator.zip